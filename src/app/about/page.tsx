import { AboutContent } from "./content";

export const metadata = {
  title: "About",
  description:
    "Learn about ElectronixBay — India's trusted source for premium refurbished business laptops.",
};

export default function AboutPage() {
  return <AboutContent />;
}
