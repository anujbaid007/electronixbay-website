import { ContactContent } from "./content";

export const metadata = {
  title: "Contact",
  description:
    "Request a quote for bulk refurbished laptops. Contact ElectronixBay for business inquiries.",
};

export default function ContactPage() {
  return <ContactContent />;
}
